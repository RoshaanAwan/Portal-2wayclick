// Stub for the Next.js 'server-only' marker so server modules can be imported
// in the vitest (Node) integration environment. In the real app this package
// throws if imported from a client bundle; in tests there is no client bundle.
export {};
